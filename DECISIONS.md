# DECISIONS.md

## D-001. Vite 기반 React SPA를 사용한다

### 결정

Vite + React + TypeScript로 단일 페이지 애플리케이션을 구성한다.

### 이유

- 과제에는 서버 렌더링, 인증, 복잡한 라우팅이 없다.
- Next.js의 서버 기능보다 낙관적 업데이트, 실패 복구, 테스트에 구현 시간을 집중할 수 있다.
- Vite와 Vitest를 함께 사용해 설정 범위를 줄인다.

### AI 제안 검토

- Next.js App Router 제안은 구현 가능하지만 이번 제품 요구보다 넓어 기각한다.

## D-002. 드래그앤드롭 대신 명시적 단계 선택과 이동 버튼을 사용한다

### 결정

카드마다 현재 단계에서 허용된 다음 단계 select와 이동 button을 제공한다.

### 이유

- Must에서 두 방식 중 하나를 선택할 수 있다.
- 키보드 대체 경로를 별도로 만들 필요가 적다.
- 동작 재현과 테스트가 결정적이다.
- 6~10시간 상정 과제에서 DnD 센서·overlay·drop target·접근성 비용을 피한다.

### 트레이드오프

- 보드 특유의 직접 조작감은 약해진다.
- 대신 상태 로직과 실패 복구 품질을 더 분명하게 보여 준다.

## D-003. TanStack Query cache를 지원자 목록의 단일 진실 공급원으로 사용한다

### 결정

지원자 목록을 별도 Zustand/Context 복제 상태로 두지 않는다.

### 이유

- API 데이터와 UI 데이터의 이중 관리로 인한 불일치를 줄인다.
- query cache에서 낙관적 변경, 성공 병합, 실패 복구를 한 흐름으로 다룬다.
- 검색, 필터, 상세 선택은 로컬 UI 상태로 충분하다.

### 기각한 대안

- Query cache와 Zustand에 같은 지원자 목록을 동시에 저장하는 구조는 동기화 책임이 늘어나 기각한다.

## D-004. MSW와 localStorage로 배포 가능한 mock API를 만든다

### 결정

UI는 실제 `fetch('/api/...')`를 호출하고 MSW가 브라우저에서 요청을 가로챈다. 데이터는 versioned localStorage key에 저장한다.

### 이유

- HTTP 상태, 지연, 실패를 UI 코드 밖에서 표현할 수 있다.
- 별도 서버 프로세스 없이 clone, 로컬 실행, 정적 배포에서 동일한 동작을 확인할 수 있다.
- 새로고침 영속성을 만족한다.

### 주의

- 실패 여부는 저장 전에 판정한다.
- 테스트에서는 랜덤 실패를 사용하지 않고 handler를 override한다.

## D-005. 전체 목록 snapshot이 아니라 실패한 지원자 엔티티만 롤백한다

### 결정

`onMutate` context에는 `previousApplicant` 한 건만 저장한다. 동일 지원자의 중복 이동은 synchronous pending guard로 막고, 서로 다른 지원자의 이동은 병렬 허용한다.

### 이유

- 서로 다른 카드 요청이 겹칠 때 오래된 전체 snapshot 복원이 다른 카드의 성공·pending 상태를 덮을 수 있다.
- 엔티티 단위 롤백은 실패 영향 범위를 해당 카드로 제한한다.
- 동일 카드 연속 이동을 큐잉하는 고급 설계보다 구현과 검증이 단순하고 결정적이다.

### 트레이드오프

- 한 카드의 요청이 완료될 때까지 그 카드의 추가 이동은 할 수 없다.
- 이번 과제에서는 데이터 무결성과 명확한 피드백을 우선한다.

## D-006. 6~10시간 범위에서는 측정되지 않은 확장 설계를 제거한다

### 결정

- GET 응답은 별도 `total` 없이 `Applicant[]`를 사용한다.
- 지원자 모델에 실제 충돌 검출에 쓰이지 않는 `version`과 별도 정렬용 `stageUpdatedAt`을 두지 않는다.
- 기본 240건은 직접 필터링·그룹화하고 `useDeferredValue`, 가상화, 검색 인덱스를 선행하지 않는다.
- 로딩 상태는 접근 가능한 문구와 최소 placeholder로 구현한다.
- Undo는 이후 D-011의 알림 연동 운영 정책으로 기각했고, 1,000건 가상화만 활성 구현 계획에서 제외한다.

### 이유

- 페이지네이션, 다중 사용자 충돌 검출, 정렬 정책은 과제 Must가 아니다.
- 240건 단순 순회는 먼저 구현하고 실제 체감 지연이 확인될 때 최적화하는 편이 검증 범위가 작다.
- 선택 기능보다 낙관적 업데이트 실패 롤백, 동시 저장, 접근성 검증에 시간을 우선 배분한다.

### 다시 검토할 조건

- 서버 페이지네이션이 추가되면 `total`을 응답 계약에 추가한다.
- 실제 다중 사용자 쓰기 충돌을 처리하면 expected version을 포함한 계약을 설계한다.
- 1,000건에서 측정 가능한 렌더링 병목이 재현되면 그때 가상화를 검토한다.

## D-007. 낙관적 업데이트와 경쟁 상태 안전성을 같은 범위에서 완성한다

### 결정

동일 지원자의 synchronous pending guard, 서로 다른 지원자의 병렬 이동, 엔티티 단위 롤백을 `optimistic-update` 범위에서 함께 구현하고 검증한다.

### 이유

- 엔티티 롤백은 동일 지원자의 요청 중첩이 차단된다는 전제에서만 안전하다.
- guard를 뒤 커밋으로 미루면 중간 커밋이 알려진 경쟁 상태를 가진 채 남는다.
- 서로 다른 PATCH는 지연·실패 판정 뒤 최신 `localStorage`를 읽고 한 엔티티만 동기적으로 저장해야 두 성공 결과가 모두 유지된다.

## D-008. mock PATCH는 입력 형태를 검증하되 요청 크기 제한은 두지 않는다

### 결정

빈·잘못된 JSON, 객체가 아닌 body, 유효하지 않은 타입·값의 stage는 저장 없이 거부한다. 별도 요청 body 크기 상한은 추가하지 않는다.

### 이유

- 이 과제의 PATCH body는 stage 문자열 하나이며 실제 공개 서버나 파일 업로드가 없다.
- 크기 제한은 Must의 신뢰성에 기여하지 않으면서 새 오류 코드와 테스트 범위를 만든다.
- 실제 서버 경계나 큰 payload가 추가될 때 인프라 수준 제한과 함께 다시 검토한다.

## D-009. 기능 스타일은 CSS Modules를 사용한다

### 결정

기능별 스타일은 CSS Modules로 두고, 전역 CSS는 reset과 공통 토큰에만 사용한다.

### 이유

- 컴포넌트 스타일 충돌을 별도 CSS-in-JS 의존성 없이 막을 수 있다.
- Vite 기본 구성에서 추가 런타임 설정이 필요 없다.

## D-010. 단계 이동 알림은 카드별 native live region으로 처리한다

### 결정

- 이동 진행과 성공은 카드별 `<p role="status">`의 polite live region으로 전달한다.
- 실패는 rollback 사실을 포함한 `<p role="alert">`로 전달한다.
- 성공과 실패 메시지는 별도 로컬 상태로 유지한다. 따라서 지원자 A의 실패 뒤 지원자 B가 성공해도 B의 성공이 A의 실패 alert를 지우지 않는다.

### 이유

- D-002의 명시적 단계 선택·이동 버튼은 키보드로 실행할 수 있지만, FR-11은 처리 결과도 인지할 수 있어야 한다.
- D-005와 D-007에 따라 카드별 이동이 병렬로 완료·실패할 수 있다. 단일 메시지 상태는 A 실패 뒤 B 성공 반례에서 A의 rollback 안내를 덮어쓴다.
- 화면 안의 native live region만으로 진행·성공·실패를 전달할 수 있다. toast 라이브러리는 새 의존성과 UI 체계를, 전역 상태는 불필요한 상태 소유권을, 자동 닫힘 타이머는 읽기 전에 메시지가 사라지는 시간 상태를 더하므로 기각한다.

### 사용자 검증 범위

- 완료: 기록된 사용자 검증에서 키보드 단계 이동, pending status와 `aria-busy`·비활성 컨트롤, 강제 성공 status, 강제 실패 rollback alert·카드 위치 복원, A 실패 뒤 B 성공 시 A alert 유지가 확인됐다.
- 미검증: VoiceOver에서 성공·실패와 여러 카드의 동시 pending 메시지가 어떤 순서로 안내되는지는 확인하지 않았다. VoiceOver로 확인된 범위는 pending status 안내뿐이다.
- Undo는 이후 D-011의 운영 정책상 기각 판단으로 대체했고, 1,000건 가상화는 D-006의 기존 미완료 판단을 유지한다.

## 범위 우선순위와 남긴 기능

### 완료 우선순위

1. Must 전체
2. 낙관적 업데이트·롤백 테스트
3. 동일 카드 경쟁 상태와 접근성

Undo는 단계 변경에 연결될 수 있는 지원자 알림과의 불일치 위험 때문에 기각한다. 1,000건 가상화만 Must가 안정된 뒤 남은 시간과 실제 필요성을 확인해 별도 기능으로 다시 계획한다.

## D-011. 일반 채용담당자 UI에는 단방향 전이만 허용한다

### 결정

`DOCUMENT_REVIEW → INTERVIEW/REJECTED`, `INTERVIEW → OFFER/REJECTED`, `OFFER → HIRED/REJECTED`만 허용한다. `HIRED`와 `REJECTED`는 종료 상태로 이동 form을 제공하지 않는다.

### 이유

- 향후 단계 변경이 지원자 알림 같은 외부 상태를 일으킬 수 있으므로, 이전 단계 복귀나 임의 건너뛰기는 UI와 외부 상태를 불일치시킬 수 있다.
- 전이표는 순수 정책 하나로 두고 UI와 mock API가 함께 사용해 직접 API 호출로 정책을 우회하지 못하게 한다.
- 상태 정정은 감사 로그를 갖춘 관리자 전용 흐름이 생길 때 검토한다.

### 기각

- Undo는 단방향 정책을 역행하고 알림 상태를 되돌릴 계약이 없으므로 FR-09 Should로 구현하지 않는다.

## D-012. 모든 허용된 단계 변경은 요청 전에 최종 확인한다

### 결정

일반 채용담당자는 허용된 목표 단계를 고른 뒤에도 native confirmation dialog에서 지원자명·현재 단계·변경 단계를 확인해야 PATCH를 실행할 수 있다.

### 이유

- 향후 지원자 알림 같은 외부 동작과 잘못된 상태 변경의 위험을 줄이는 안전장치다.
- 취소·Esc는 어떤 상태도 변경하지 않고, 확인 후에는 검증된 기존 optimistic move 흐름을 재사용한다.

### 범위와 기각

- 실제 알림 발송이나 감사 로그는 구현하지 않는다.
- `window.confirm`, 새 dialog 라이브러리, 전역 확인 상태는 사용하지 않는다.
- 관리자 상태 정정, 알림 발송 실패 정책, 감사 로그는 별도 scope에서 다룬다.

### 미완료 기능 기록 형식

```md
### 남긴 기능: <scope>

- 시도한 범위:
- 중단한 이유:
- 현재 코드에 미완성 흔적을 남겼는지:
- 이어서 구현한다면 첫 단계:
- Must 결과물에 미치는 영향:
```
